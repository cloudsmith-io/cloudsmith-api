/* tslint:disable */
/* eslint-disable */
/**
 * Cloudsmith API (v1)
 * The API to the Cloudsmith Service
 *
 * The version of the OpenAPI document: v1
 * Contact: support@cloudsmith.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * The dynamic mappings of `mapping_claim` values to service accounts. Cannot be provided if `service_accounts` is also set.
 * 
 * Note: This field and the dynamic mappings feature are still in early access. Breaking changes are possible as we receive feedback on this feature.
 * @export
 * @interface DynamicMapping
 */
export interface DynamicMapping {
    /**
     * The OIDC token claim value that must be present in the token for it to successfully authenticate as the mapped `service_account`.
     * 
     * Note: This field and the dynamic mappings feature are still in early access. Breaking changes are possible as we receive feedback on this feature.
     * @type {string}
     * @memberof DynamicMapping
     */
    claim_value: string;
    /**
     * The service account associated with the provider setting and `claim_value`
     * 
     * Note: This field and the dynamic mappings feature are still in early access. Breaking changes are possible as we receive feedback on this feature.
     * @type {string}
     * @memberof DynamicMapping
     */
    service_account: string;
}

/**
 * Check if a given object implements the DynamicMapping interface.
 */
export function instanceOfDynamicMapping(value: object): value is DynamicMapping {
    if (!('claim_value' in value) || value['claim_value'] === undefined) return false;
    if (!('service_account' in value) || value['service_account'] === undefined) return false;
    return true;
}

export function DynamicMappingFromJSON(json: any): DynamicMapping {
    return DynamicMappingFromJSONTyped(json, false);
}

export function DynamicMappingFromJSONTyped(json: any, ignoreDiscriminator: boolean): DynamicMapping {
    if (json == null) {
        return json;
    }
    return {
        
        'claim_value': json['claim_value'],
        'service_account': json['service_account'],
    };
}

export function DynamicMappingToJSON(json: any): DynamicMapping {
    return DynamicMappingToJSONTyped(json, false);
}

export function DynamicMappingToJSONTyped(value?: DynamicMapping | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'claim_value': value['claim_value'],
        'service_account': value['service_account'],
    };
}

